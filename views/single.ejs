<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %></title>
    <link rel="icon" type="image/png" href="/smileyslimeyboy.png">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="header">
        <nav>
            <a href="/ss">notes</a>
            <a href="/">essays</a>
            <a href="/about">about</a>
        </nav>
        <a href="/admin/login" class="login-link" style="color: #333; text-decoration: none; font-size: 0.95em; text-transform: lowercase;">login</a>
    </div>

    <div class="container">
        <div class="post">
            <h2 class="post-title"><%= post.title %></h2>
            <% const preview = getPreview(post.content); %>
            <% if (preview) { %>
                <span class="post-subheading"><%= preview %></span>
            <% } %>
            <span class="post-date"><%= new Date(post.date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %></span>
            <div class="post-content"><%- formatContent(post.content) %></div>
        </div>

        <div style="margin-top: 30px;">
            <a href="<%= post.type === 'note' ? '/ss' : '/' %>" style="color: #333; text-decoration: none; font-size: 0.9em;">← back</a>
        </div>
    </div>

    <div class="footer">
    </div>
</body>
</html>
